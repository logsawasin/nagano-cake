<h1>商品一覧</h1>

<p><%= link_to "商品登録", new_admin_item_path %></p>

<% @items.each do |item| %>
   <%= item.name %>  
   <%= item.text %>
   <%= item.without_tax_price %>
   <%= image_tag item.image, size: "200x200" %>
   <%= link_to "商品編集",admin_item_path(item.id) %>
<% end %>
